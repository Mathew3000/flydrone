# gym-pybullet-drones v1.0.0 (2021 vintage), patched to run on Python 3.10
# with a current numpy/scipy -- see PATCHES.md.
numpy>=2
scipy
Pillow
matplotlib
cycler
gym==0.26.2
pybullet
opencv-python-headless

# Optional: nur fuer den echten MaleCNS-Konnektom-Zugriff.
# - pandas, pyarrow: lokale Bulk-Dateien lesen (connectome/local_maleCNS.py)
# - neuprint-python: einzelne Ad-hoc-Queries gegen die neuPrint-API
#   (connectome/data_loader.py::fetch_from_neuprint)
# Nicht noetig fuer Sanity-Check/M1/M2 mit dem synthetischen Testnetz.
pandas
pyarrow
neuprint-python
